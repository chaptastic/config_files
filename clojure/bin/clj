#!/bin/bash

BREAK_CHARS="(){}[],^%$#@\"\";:''|\\"
if [ $# -eq 0 ]; then 
     rlwrap --remember -c -b $BREAK_CHARS -f $HOME/.clj_completions \
     java -cp $CLOJURE_CP clojure.lang.Repl
else
     java -cp $CLOJURE_CP clojure.lang.Script $1 -- $@
fi
